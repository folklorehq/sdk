/**
 * @example
 *     {
 *         contact_id: "contact_id"
 *     }
 */
export interface ListContactNotesRequest {
    /** The unique identifier of a contact. */
    contact_id: string;
    /** The page of results to fetch. Defaults to first page */
    page?: number;
    /** How many results to display per page. Defaults to 15 */
    per_page?: number;
}
