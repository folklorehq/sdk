import type * as Intercom from "../../../../../index.mjs";
/**
 * A paginated list of macros (saved replies) in the workspace.
 */
export interface MacroList {
    /** Always list */
    type?: "list";
    /** The list of macro objects */
    data?: (Intercom.unstable.Macro | undefined)[];
    /** Pagination information */
    pages?: MacroList.Pages;
}
export declare namespace MacroList {
    /**
     * Pagination information
     */
    interface Pages {
        /** The type of pagination */
        type?: "pages";
        /** Number of results per page */
        per_page?: number;
        /** Cursor for the next page */
        next?: Pages.Next;
    }
    namespace Pages {
        /**
         * Cursor for the next page
         */
        interface Next {
            /** Base64-encoded cursor containing [updated_at, id] for pagination */
            starting_after?: string;
        }
    }
}
