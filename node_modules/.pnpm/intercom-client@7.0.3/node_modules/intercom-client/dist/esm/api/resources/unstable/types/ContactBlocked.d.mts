import type * as Intercom from "../../../index.mjs";
/**
 * blocked contact object
 */
export interface ContactBlocked extends Intercom.unstable.ContactReference {
    /** Always true. */
    blocked?: boolean;
}
