/**
 * Register a Fin Voice call with Intercom
 */
export interface RegisterFinVoiceCallRequest {
    /** Phone number in E.164 format for the call */
    phone_number: string;
    /** External call identifier from the call provider */
    call_id: string;
    /** Source of the call. Can be "five9", "zoom_phone", or defaults to "aws_connect" */
    source?: RegisterFinVoiceCallRequest.Source;
    /** Additional metadata about the call */
    data?: Record<string, unknown>;
}
export declare namespace RegisterFinVoiceCallRequest {
    /** Source of the call. Can be "five9", "zoom_phone", or defaults to "aws_connect" */
    const Source: {
        readonly Five9: "five9";
        readonly ZoomPhone: "zoom_phone";
        readonly AwsConnect: "aws_connect";
    };
    type Source = (typeof Source)[keyof typeof Source];
}
