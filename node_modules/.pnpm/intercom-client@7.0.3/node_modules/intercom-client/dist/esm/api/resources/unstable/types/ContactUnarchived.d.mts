import type * as Intercom from "../../../index.mjs";
/**
 * unarchived contact object
 */
export interface ContactUnarchived extends Intercom.unstable.ContactReference {
    /** Whether the contact is archived or not. */
    archived?: boolean;
}
