import type * as Intercom from "../../../../../index.mjs";
/**
 * Data related to AI Agent involvement in the conversation.
 */
export interface AiAgent {
    /** The type of the source that triggered AI Agent involvement in the conversation. */
    source_type?: AiAgent.SourceType;
    /** The title of the source that triggered AI Agent involvement in the conversation. If this is `essentials_plan_setup` then it will return `null`. */
    source_title?: string;
    /** The type of the last answer delivered by AI Agent. If no answer was delivered then this will return `null` */
    last_answer_type?: AiAgent.LastAnswerType;
    /** The resolution state of AI Agent. If no AI or custom answer has been delivered then this will return `null`. */
    resolution_state?: AiAgent.ResolutionState;
    /** The customer satisfaction rating given to AI Agent, from 1-5. */
    rating?: number;
    /** The customer satisfaction rating remark given to AI Agent. */
    rating_remark?: string;
    /** The time when the AI agent rating was created. */
    created_at?: number;
    /** The time when the AI agent rating was last updated. */
    updated_at?: number;
    content_sources?: Intercom.unstable.ContentSourcesList;
}
export declare namespace AiAgent {
    /** The type of the source that triggered AI Agent involvement in the conversation. */
    const SourceType: {
        readonly EssentialsPlanSetup: "essentials_plan_setup";
        readonly Profile: "profile";
        readonly Workflow: "workflow";
        readonly WorkflowPreview: "workflow_preview";
        readonly FinPreview: "fin_preview";
    };
    type SourceType = (typeof SourceType)[keyof typeof SourceType];
    /** The type of the last answer delivered by AI Agent. If no answer was delivered then this will return `null` */
    const LastAnswerType: {
        readonly AiAnswer: "ai_answer";
        readonly CustomAnswer: "custom_answer";
    };
    type LastAnswerType = (typeof LastAnswerType)[keyof typeof LastAnswerType];
    /** The resolution state of AI Agent. If no AI or custom answer has been delivered then this will return `null`. */
    const ResolutionState: {
        readonly AssumedResolution: "assumed_resolution";
        readonly ConfirmedResolution: "confirmed_resolution";
        readonly Escalated: "escalated";
        readonly NegativeFeedback: "negative_feedback";
        readonly ProcedureHandoff: "procedure_handoff";
    };
    type ResolutionState = (typeof ResolutionState)[keyof typeof ResolutionState];
}
