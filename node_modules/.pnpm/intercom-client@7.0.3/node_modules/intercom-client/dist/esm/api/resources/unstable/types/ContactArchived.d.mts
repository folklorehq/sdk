import type * as Intercom from "../../../index.mjs";
/**
 * archived contact object
 */
export interface ContactArchived extends Intercom.unstable.ContactReference {
    /** Whether the contact is archived or not. */
    archived?: boolean;
}
