import type * as Intercom from "../../../index.mjs";
export type RedactConversationRequest = 
/**
 * Payload of the request to redact a conversation part */
Intercom.unstable.RedactConversationRequest.ConversationPart
/**
 * Payload of the request to redact a conversation source */
 | Intercom.unstable.RedactConversationRequest.Source;
export declare namespace RedactConversationRequest {
    interface ConversationPart {
        type: "conversation_part";
        /** The id of the conversation. */
        conversation_id: string;
        /** The id of the conversation_part. */
        conversation_part_id: string;
    }
    interface Source {
        type: "source";
        /** The id of the conversation. */
        conversation_id: string;
        /** The id of the source. */
        source_id: string;
    }
}
