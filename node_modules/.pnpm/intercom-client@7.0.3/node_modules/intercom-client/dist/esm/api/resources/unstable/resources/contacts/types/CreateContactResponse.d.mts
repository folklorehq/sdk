import type * as Intercom from "../../../../../index.mjs";
export interface CreateContactResponse extends Intercom.unstable.Contact {
    /** If the user has enabled push messaging. */
    enabled_push_messaging?: boolean;
}
